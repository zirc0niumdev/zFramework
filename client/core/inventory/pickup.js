onNet('Client.Pickup.CreatePickup', (action, item) => {
    switch (action) {
        case 1: // ? Pickup
            
            break;

        case 2: // ? Pickup
                
            break;
            
        case 3: // Delete Pickup
                
            break;
            
        case 4: // Create Pickup
                    
            break;
    
        default:
            break;
    }
});